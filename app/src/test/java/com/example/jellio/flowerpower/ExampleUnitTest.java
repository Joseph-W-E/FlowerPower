package com.example.jellio.flowerpower;

import android.graphics.Bitmap;
import android.graphics.Color;
import android.graphics.drawable.Drawable;

import org.junit.Test;

import static org.junit.Assert.*;

/**
 * To work on unit tests, switch the Test Artifact in the Build Variants view.
 */
public class ExampleUnitTest {

    @Test
    public void withinThresholdTest() {
        /*
        PlantHealthAnalyzer pha = new PlantHealthAnalyzer();
        assertTrue(pha.withinThreshold(0, 100, 0)); // dark green
        assertTrue(pha.withinThreshold(34, 139, 34)); // forest green
        assertTrue(pha.withinThreshold(160, 83, 45)); // sienna (brown)

        assertFalse(pha.withinThreshold(255, 105, 180)); // hot pink
        assertFalse(pha.withinThreshold(124, 252, 0)); // lawn green
        assertFalse(pha.withinThreshold(32, 178, 170)); // light sea green
        assertFalse(pha.withinThreshold(102, 205, 170)); // medium aquamarine
        assertFalse(pha.withinThreshold(255, 0, 0)); // red
        assertFalse(pha.withinThreshold(0, 0, 0)); // black
        assertFalse(pha.withinThreshold(119, 136, 153)); // light slate gray
        */
    }

}